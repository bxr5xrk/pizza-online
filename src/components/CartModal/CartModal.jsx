import React from "react";
import st from "./CartModal.module.scss";

const CartModal = () => {
    return (
        <div className={st.wrapper}>
            modal window <input type="text" placeholder="enter email" />{" "}
        </div>
    );
};

export default CartModal;
